<ul>
  {{#each coasters}}
    {{#unless rode}}
    <button class = "change-rode" data-id = "{{id}}" data-newrode="{{rode}}">
      {{coaster_name}}
    </button>
    {{/unless}}
  {{/each}}
</ul>

<ul>
  {{#each coasters}}
    {{#if rode}}
      <button class = "change-rode" data-id = "{{id}}" data-newrode="{{rode}}">
      {{coaster_name}}
    </button>
    {{/if}}
  {{/each}}
</ul>

<form class="create-form">

  <div class="form-group">
    <label for="ca">Coaster Name:</label>
    <input type="text" id="coaster" name="name">
  </div>
  <button type="submit">Add</button>
</form>

